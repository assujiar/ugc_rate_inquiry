## Environment variables for Rate Request app
## Copy this file to .env.local and fill in the values.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role key is used only in server-side functions (API routes) that need elevated permissions
# DO NOT expose this key to the browser.
SUPABASE_SERVICE_ROLE_KEY=

# Optional: base URL for building absolute links (e.g. for emails)
NEXT_PUBLIC_APP_URL=http://localhost:3000