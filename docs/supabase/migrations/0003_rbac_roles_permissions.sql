-- Migration: 0003_rbac_roles_permissions.sql
-- Description: Define roles, permissions and role_permissions tables.

-- Placeholder: create tables roles(id, name), permissions(id, description), role_permissions(role_id, permission_id).